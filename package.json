{
  "name": "cyclecare",
  "version": "1.0.0",
  "private": true,
  "description": "Menstrual cycle calculator - installable Android app",
  "scripts": {
    "sync": "cap sync",
    "build:android": "npm run sync && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
